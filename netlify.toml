[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Public backend API (Railway). Baked into the bundle at build time.
  VITE_API_BASE_URL = "https://api-production-38d4.up.railway.app/api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
